namespace GameData.buffs
{
    public enum Stat : byte
    {
        // Vitals
        MHP, HP, MMP, MP,

        // Stats
        ATK,
        DEF,
        MATK,
        MDEF,
        SPEED,
        ACCURACY,
        MOVE,
        WEIGHT,
    }
}
