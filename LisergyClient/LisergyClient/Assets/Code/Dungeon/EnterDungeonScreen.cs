using Game.Events.Bus;
using UnityEngine;

public class EnterDungeonScreen : MonoBehaviour
{
    public PartyAssignmentUI UI;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
